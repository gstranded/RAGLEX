# Flask 配置
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production

# 数据库配置
# 生产/默认示例（MySQL）
DATABASE_URL=mysql+pymysql://root:Mysql123!@localhost:3306/raglex_law

# 本地最小启动（无需 MySQL）：
# - run_backend_sqlite.sh 会自动设置 DEV_DATABASE_URL/DATABASE_URL
# - 或者你也可以手动启用：
# DEV_DATABASE_URL=sqlite:///data/raglex-dev.sqlite3

# JWT 配置
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production

# MinIO 配置
# 本地最小启动（无需 MinIO）：
# MINIO_DISABLED=True
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=raglex-documents
MINIO_SECURE=False

# CORS 配置
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# 应用配置
MAX_CONTENT_LENGTH=52428800  # 50MB
UPLOAD_FOLDER=uploads

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# 分页配置
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# 缓存配置（可选）
CACHE_TYPE=simple
CACHE_DEFAULT_TIMEOUT=300

# Redis 配置（如果使用 Redis 缓存）
# REDIS_URL=redis://localhost:6379/0

# 邮件配置（如果需要邮件功能）
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password